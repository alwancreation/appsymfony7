{% extends 'admin/base.html.twig' %}

{% block body %}
<!-- Page header -->
<div class="page-header">
    <div class="page-title">
        <h3>
            {% trans from 'admin' %}New home section{% endtrans %}
            <small>{% trans from 'admin' %}New home section{% endtrans %}</small>
        </h3>
    </div>

</div>
<!-- Breadcrumbs line -->
<div class="breadcrumb-line">
    <ul class="breadcrumb">
        <li><a href="{{ url('admin_default_index') }}">{% trans from 'admin' %}Home{% endtrans %}</a></li>
        <li><a href="{{ url('pages_index') }}">{% trans from 'admin' %}Pages{% endtrans %}</a></li>
        <li><a href="{{ url('pages_edit',{'id':page.pageId,"lang":editable_lang}) }}">{{ page.pageTitle }}</a></li>
        <li class="active">{% trans from 'admin' %}New home section{% endtrans %}</li>
    </ul>
</div>
<div class="content">
<div class="panel panel-default">
	<div class="panel-body">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary" value="{% trans from 'admin' %}Create{% endtrans %}" />
        {{ form_end(form) }}
	</div>
</div>

</div>
    <div class="panel panel-default">
    	<div class="panel-body">
            <a href="{{ url('pages_edit',{'id':page.pageId,"lang":editable_lang}) }}" class="btn btn-default">{% trans from 'admin' %}Back to the list{% endtrans %}</a>
    	</div>
    </div>
{% endblock %}
